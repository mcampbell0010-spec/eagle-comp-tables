# SEC EDGAR requires identifiable contact info on every API request.
# Set this to your name and email address before running anything that
# pulls from EDGAR (build_comp_table.py, verify_against_edgar.py).
#
# Copy this file to .env (which is gitignored) and fill in your details.

SEC_USER_AGENT=Your Name your.email@example.com
